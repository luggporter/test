name: Update Notion with GitHub Releases

on:
  push:
    branches:
      - main  # 변경 필요

jobs:
  update-notion:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8  # 변경 가능

    - name: Install dependencies
      run: |
        pip install requests

    - name: Run the script
      env:
        GITHUB_API_TOKEN: ${{ secrets.GITHUB_API_TOKEN }}
        NOTION_API_TOKEN: 'secret_XWyDXf8udaiO3SAxejWq5RdMz5LEDniw30s64NX9bjT'
        # NOTION_API_TOKEN: ${{ secrets.NOTION_API_TOKEN }}
        NOTION_DATABASE_ID: '62c7749c45a9408aa4c720747b5addb3'  # 변경 가능
      run: python ../notionScript.py  # 스크립트 경로 변경
